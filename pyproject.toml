[tool.codespell]
skip = "*.min.js,docs-site/package-lock.json"

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
markers = [
  "integration: integration tests requiring a real MA server instance",
]
filterwarnings = [
  "ignore:coroutine.*was never awaited:RuntimeWarning",
]
